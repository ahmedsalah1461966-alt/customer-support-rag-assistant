# Core Hugging Face stack (pinned together to avoid version-mismatch
# ImportErrors between huggingface_hub / transformers / tokenizers / datasets)
huggingface_hub>=0.26.0,<0.28.0
transformers>=4.46.0,<4.47.0
tokenizers>=0.20.0,<0.21.0
datasets>=3.0.0,<3.2.0
accelerate>=1.0.0,<1.2.0

# LangChain stack
langchain>=0.3.20,<0.4
langchain-core>=0.3.40,<0.4
langchain-community>=0.3.20,<0.4
langchain-huggingface>=0.1.2,<0.4
langchain-text-splitters>=0.3.5,<0.4

# Vector search, embeddings, UI
faiss-cpu>=1.8.0
sentence-transformers>=3.0.0
gradio>=4.36.0

# Data handling (usually already present, listed for a clean venv)
pandas>=2.0.0
torch>=2.1.0
